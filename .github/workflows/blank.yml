name: API Tests

on:
  schedule:
    - cron: "0 0 * * *"  # Запуск каждый день в 00:00 UTC
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v3

      - name: 🔧 Установка зависимостей (Newman + репортеры)
        run: |
          echo "🔧 Устанавливаем зависимости..."
          npm install -g newman newman-reporter-html newman-reporter-json
          
          echo "🔍 Проверяем установленные пакеты..."
          npm list -g newman || { echo "❌ newman не установлен!"; exit 1; }
          npm list -g newman-reporter-html || { echo "❌ newman-reporter-html не установлен!"; exit 1; }
          npm list -g newman-reporter-json || { echo "❌ newman-reporter-json не установлен!"; exit 1; }

      - name: 🔍 Проверка установки newman
        run: |
          echo "🔍 Проверяем newman..."
          command -v newman || { echo "❌ newman не найден!"; exit 1; }
          newman -v || { echo "❌ Ошибка: newman не работает!"; exit 1; }

      - name: 🔍 Проверка доступных репортеров
        run: newman -h | grep "reporters" || echo "❌ Репортеры не найдены!"

      - name: 🚀 Запуск API-тестов и создание отчетов
        run: |
          mkdir -p public
          echo "🛠 Запускаем newman..."
          newman run postman_collection_auth_fixed.json \
            -e Investudy.postman_environment.json \
            -r html --reporter-html-export=public/report.html \
            -r json --reporter-json-export=newman-report.json \
            -r cli --verbose | tee newman.log || true

      - name: 📂 Проверка наличия отчетов
        run: |
          echo "📂 Проверяем созданные файлы..."
          ls -la public/ || echo "❌ Папка public пуста!"
          ls -la newman-report.json || echo "❌ JSON-отчет не найден!"
          echo "📄 Вывод последних 20 строк newman.log:"
          cat newman.log | tail -20
